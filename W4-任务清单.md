# CodeRecall W4 任务清单

> **四方分工**：Codex（主工程）· Gemini（内容/文档）· Claude（审阅）· **你（人类）**（执行/录制/决策）  
> **基线**：pytest 75 · vitest 28 · tsc 0 error · build 成功  
> **唯一 P0**：AI 端点 `wzw.pp.ua` 不可达，D1 优先处理

---

## Day 1 — AI 端点确认 + 工程修复 + Demo 数据

### 🔵 Codex
- [ ] 修复 `get_overview` 删除无效 `days` 参数（`stats_service.py` + `stats.py` route）
- [ ] 在 `selector.py` 加注释说明 `due_first`/`spaced_repetition` 当前等价原因
- [ ] 在 `ai_analysis_service.py` 加 `resolve_model()` allowlist 校验 + `LLM_ALLOWED_MODELS` env
- [ ] 实现后端 mock provider（`LLM_PROVIDER=mock` 时流式返回预设 4 小节 Markdown）
- [ ] 运行/完善 `seed_demo_data.py`：60 道题 / 6 分类 / 90 天日志 / ~400 条 review log

### 👤 你（人类）
- [ ] **检查 `wzw.pp.ua` 是否可达**：`curl https://wzw.pp.ua/v1/models` 或直接在浏览器访问
- [ ] **决策 AI 端点方案**：
  - 可达 → 无需操作，Codex 正常配置
  - 不可达 → 告知 Codex 切换到 `LLM_BASE_URL=https://api.openai.com/v1` 并提供新 API Key
  - 无 Key → 用 mock provider 演示，`ENABLE_AI_ANALYSIS=false` 作兜底

---

## Day 2 — 全链路联调 + UX 扫雷

### 🔵 Codex
- [ ] 全链路手测：Dashboard → due-count → start session → reveal → submit → summary → stats 刷新
- [ ] 边界 case：空库无白屏、全部掌握后 Dashboard 空态、AI 超时主流程不中断
- [ ] 后端 4xx/5xx 前端有明确错误提示
- [ ] 运行 `pytest` + `vitest` + `tsc` 确认基线不回退

### 🟡 Gemini
- [ ] 输出 UX 扫雷清单（已完成版本在 W4 CCG 报告中），交给人类执行

### 👤 你（人类）
- [ ] **按 UX 清单逐条手动验证**（需要人眼判断的视觉问题）：
  - 打开 Chrome DevTools，走完整流程，确认 Console **无红色 Error**
  - 切换 Dark Mode，检查图表坐标轴/Tooltip 颜色是否跟随变化
  - AI 流式输出时，容器是否**自动滚动到底部**
  - 所有错题复习完后，Dashboard "今日已完成"空态是否好看
  - DiffViewer 有无横向滚动条（检查演示用题目的代码行长度）

---

## Day 3 — 数据一致性 + 演示彩排

### 🔵 Codex
- [ ] 检查 SM-2 字段一致性（ease_factor / interval / next_review_at 更新链）
- [ ] 为 `shown_at` / `next_review_at` / `is_archived` 补 SQLite 索引（Alembic 迁移）
- [ ] 验证 import/export v2 round-trip 核心字段不丢失

### 🟡 Gemini
- [ ] 输出优化后的演示视频黄金路径剧本 v2（含每个操作步骤 + 旁白台词 + 精确时间点）
- [ ] 输出录制工具建议和画面参数说明（分辨率/帧率/窗口大小/鼠标高亮工具）

### 👤 你（人类）
- [ ] **按 Gemini 的剧本进行 3 次彩排**（不录制，只走流程）：
  - 第 1 次：熟悉操作顺序，记录卡顿点
  - 第 2 次：配合旁白台词，计时确认 5 分钟内
  - 第 3 次：确认鼠标轨迹自然、无多余点击
- [ ] 确定演示用的具体错题（React Hooks 闭包 / Python 算法 / 至少 2 道）
- [ ] 调整好录制窗口大小和浏览器缩放比例

---

## Day 4 — 打包发布 + 视频录制

### 🔵 Codex
- [ ] 整理 `.env.example`（明确 AI provider / base URL / model allowlist / feature flag）
- [ ] 写 release runbook（迁移+seed+启动+验收命令）
- [ ] `npm run build` 生产包验证（dark mode / Monaco / Recharts / 路由刷新）
- [ ] 后端关键日志补齐（AI 请求完成、AI fallback、导入校验失败）

### 🟡 Gemini
- [ ] 输出答辩 PPT 完整内容文案（10-12 页，含每页标题 + 要点 + 建议配图说明）
- [ ] 输出 README 2.0 全文（含 Badge / Hero Image 说明 / Features / Quick Start）

### 👤 你（人类）
- [ ] **正式录制演示视频**（AI 无法代劳）：
  - 工具：macOS QuickTime（自带）或 OBS
  - 参数：1080P / 60fps，浏览器全屏或固定窗口
  - 按剧本操作，录 2-3 条备用
- [ ] **剪辑视频**：
  - 工具：iMovie（简单）/ CapCut / DaVinci Resolve
  - 剪掉空白等待、鼠标乱飞片段
  - 按 Gemini 台词文案添加字幕或配音
  - 导出 MP4，控制在 5 分钟内
- [ ] **制作 PPT**：
  - 按 Gemini 的内容文案，在 Keynote / PowerPoint / Canva 中完成排版
  - 嵌入 DiffViewer + AI 流式输出的截图/GIF

---

## Day 5 — 最终验收 + 结项

### 🔵 Codex
- [ ] 最终验收 checklist（见下）全部通过
- [ ] 用干净数据库执行迁移 + seed + 全链路手测
- [ ] 标记 W4 完成范围和已知限制文档

### 🟢 Claude
- [ ] 最终 PR 级审阅：跨文件一致性、`.env.example` 敏感信息检查、runbook 可重复性

### 🟡 Gemini
- [ ] 输出未来扩展方案文档（VS Code 插件 / 多模态 OCR）
- [ ] 输出项目结项报告（技术总结 + CCG 三方协作模式复盘）

### 👤 你（人类）
- [ ] **最终视频审看**：确认视频无黑屏、无音频问题、字幕无错别字
- [ ] **答辩准备**：熟悉 PPT 顺序，准备可能被问的问题回答
- [ ] **提交/归档**：上传视频、提交代码、整理 docs/ 文档

---

## 最终验收 Checklist（D5 由 Codex 执行，你确认）

```
后端
□ alembic upgrade head 从空库成功
□ pytest ≥75 passed

前端
□ vitest ≥28 passed
□ tsc --noEmit 0 error
□ npm run build 成功

功能
□ 错题 CRUD 正常
□ 导入导出 v2 round-trip 核心字段不丢失
□ Dashboard due-count = due_first session 抽题数量
□ submit 后 SM-2 字段正确更新
□ Stats 4 接口正常（overview/trend/heatmap/top-weak）

演示
□ Dark Mode 切换主页面无明显错色
□ DiffViewer Monaco 正常加载
□ AI 可达时 SSE 流式 4 小节返回
□ AI 不可达时主流程不中断
□ seed 数据图表有趋势/热力/弱项分布

交付
□ .env.example 无真实密钥，注释清晰
□ README/runbook 与实际启动步骤一致
□ 演示视频 ≤5 分钟，画质清晰
□ 答辩 PPT ≤12 页，内容自洽
```

---

## 四方角色速查

| 角色 | 能做 | 不能做 |
|------|------|--------|
| 🔵 Codex | 写代码、跑测试、改配置、seed 数据 | 看屏幕视觉效果、做人工判断 |
| 🟡 Gemini | 写脚本/文案/PPT 内容/README | 操作浏览器、录制视频、排版 |
| 🟢 Claude | 代码审阅、架构裁决 | 持续开发、高频修改 |
| 👤 你 | **录制视频、剪辑、制作 PPT、API Key 决策、视觉验收** | — |
